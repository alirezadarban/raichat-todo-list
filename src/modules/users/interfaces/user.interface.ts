export interface SignupInput {
    username: string;
    password: string;
}

export interface SigninInput {
    username: string;
    password: string;
}

export interface ProfileInput {
    id: string;
}